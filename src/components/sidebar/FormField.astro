---
// src/components/FormField.astro
interface Props {
  label: string;
  class?: string;
}
const { label, class: className } = Astro.props;
---
<div class:list={["flex flex-col space-y-2", className]}>
  <label class="text-sm font-medium text-gray-300">{label}</label>
  <slot />
</div>